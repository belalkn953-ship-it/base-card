const {createClient}=window.supabase;
const sb=createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);
const $=s=>document.querySelector(s);
let config={games:[],packages:[],settings:{}}, currentUser=null;
const labels={topup:'إضافة رصيد',purchase:'شراء من المحفظة',adjustment:'تعديل إداري'};
const sectionNames={shop:'الألعاب والباقات',wallet:'المحفظة والنقاط',track:'تتبع الطلب',support:'التواصل مع الدعم',order:'إنشاء طلب شحن',chatapps:'تطبيقات الدردشة',transfer:'تحويل الرصيد'};const sectionTargets={home:null,...sectionNames,topup:'wallet',games:'shop'};
function safeSectionUrl(v){const u=String(v||'').trim();return /^(https?:\/\/|\/|#)/i.test(u)?u:''}
function loadDynamicSections(rows){document.querySelectorAll('[data-custom-section],.custom-section-card').forEach(x=>x.remove());const grid=document.querySelector('.quick-sections-grid');const back=document.querySelector('#sectionBack');if(!grid||!back)return;(rows||[]).forEach(x=>{const key='custom-'+x.slug;sectionNames[key]=x.title;sectionTargets[key]=key;const href='#'+key;const btn=safeSectionUrl(x.button_url);grid.insertAdjacentHTML('beforeend',`<a class="quick-section-card custom-section-card" data-custom-section href="${href}"><span class="quick-icon">${esc(x.icon||'✦')}</span><strong>${esc(x.title)}</strong><small>${esc(x.description||'')}</small><b>دخول القسم ←</b></a>`);back.insertAdjacentHTML('afterend',`<section class="section custom-section" data-main-section="${key}" data-custom-section><div class="container"><div class="section-title"><div><label>قسم مستقل</label><h2>${esc(x.icon||'✦')} ${esc(x.title)}</h2></div><p>${esc(x.description||'')}</p></div><div class="panel custom-section-panel"><div class="custom-content">${esc(x.content||'')}</div>${btn&&x.button_text?`<a class="btn" href="${esc(btn)}" target="_blank" rel="noopener">${esc(x.button_text)} ←</a>`:''}</div></div></section>`)});}
document.addEventListener('click',e=>{const a=e.target.closest?.('a[href="#order"]');if(!a)return;e.preventDefault();const g=a.dataset.game;if(g){const game=document.getElementById('gameId');if(game){game.value=g;game.dispatchEvent(new Event('change'));}}openMainSection('order',true)});window.addEventListener('hashchange',()=>{const h=location.hash.slice(1),t=sectionTargets[h];openMainSection(t||null,false,t&&h!==t?h:'')});
function openMainSection(name,writeHash=true,subId=''){const inside=!!sectionNames[name];document.body.classList.toggle('inside-section',inside);document.querySelectorAll('[data-main-section]').forEach(x=>{const show=inside&&x.dataset.mainSection===name;x.classList.toggle('section-hidden',!show);x.style.display=show?'block':'none'});const home=$('#sectionHome');if(home){home.classList.toggle('section-hidden',inside);home.style.display=inside?'none':'block'}const back=$('#sectionBack');if(back){back.classList.toggle('hidden',!inside);back.style.display=inside?'flex':'none'}if($('#sectionHeading'))$('#sectionHeading').textContent=inside?sectionNames[name]:'';if(writeHash){history.pushState({},'',inside?'#'+name:location.pathname+location.search)}if(inside){window.scrollTo({top:0,behavior:'smooth'});if(name==='track')loadTrackOrders();if(subId)setTimeout(()=>document.getElementById(subId)?.scrollIntoView({behavior:'smooth',block:'start'}),120)}else window.scrollTo({top:0,behavior:'smooth'})}


function show(id,text,type=''){const el=$(id);if(!el)return;el.textContent=text;el.className=`alert ${type}`;el.classList.remove('hidden');}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
const requiredFields={game_id:'اختر اللعبة أولًا',package_id:'اختر الباقة أولًا',player_id:'اكتب Player ID أولًا',customer_name:'اكتب اسم العميل أولًا'};
Object.entries(requiredFields).forEach(([n,m])=>{const el=document.querySelector(`[name="${n}"]`);if(el){el.addEventListener('invalid',()=>el.setCustomValidity(m));el.addEventListener('input',()=>el.setCustomValidity(''));el.addEventListener('change',()=>el.setCustomValidity(''))}});
function money(n,c){return c==='SYP'?`${Number(n||0).toLocaleString('ar-SY')} ل.س`:`${Number(n||0).toFixed(2)} $`}
function whatsappNumber(v){let n=String(v||'').replace(/\D/g,'');if(n.startsWith('00'))n=n.slice(2);if(n.startsWith('0'))n='963'+n.slice(1);else if(!n.startsWith('963'))n='963'+n;return n}
function isServer2Item(item){const t=[item?.name,item?.slug,item?.description,item?.provider_key,item?.category_name,item?.parent_name].filter(Boolean).join(' ');return /(?:server\s*[_-]?\s*2|سيرفر\s*2)/i.test(String(t))}
async function loadConfig(){
 const [s,g,p]=await Promise.all([sb.from('settings').select('key,value'),sb.from('games').select('*').eq('visible',true).order('sort_order'),sb.from('packages').select('*,games!inner(visible)').eq('visible',true).eq('games.visible',true).order('sort_order')]);
 let sec={data:[]}; try { sec=await sb.from('site_sections').select('*').eq('visible',true).order('sort_order').order('id'); } catch (_) {}
 if(s.error||g.error||p.error)throw new Error('تعذر تحميل بيانات الموقع');
 const publicGames=(g.data||[]).filter(x=>!isServer2Item(x));
 const publicPackages=(p.data||[]).filter(x=>!isServer2Item(x)&&!isServer2Item(x.games));
 config={settings:Object.fromEntries((s.data||[]).map(x=>[x.key,x.value])),games:publicGames,packages:publicPackages,sections:sec.data||[]};window.__baseSettings=config.settings;
 loadDynamicSections(config.sections);
 const x=config.settings;if($('#brandName'))$('#brandName').textContent=x.site_name||'Base Card';if($('#brandTagline'))$('#brandTagline').textContent=x.tagline||'';if($('#paymentInfo'))$('#paymentInfo').textContent=x.sham_cash||'اشحن رصيدك من قسم شراء النقاط أولًا.';if($('#walletPaymentInfo'))$('#walletPaymentInfo').textContent=x.sham_cash||'سيتم عرض بيانات شام كاش هنا بعد ضبطها من لوحة الإدارة.';
 if(x.sham_cash_qr&&$('#paymentQr')&&$('#qrBox')){$('#paymentQr').src=x.sham_cash_qr;$('#qrBox').classList.remove('hidden')}
 const wa=whatsappNumber(x.support_whatsapp);if(wa&&$('#whatsappLink')){$('#whatsappLink').href=`https://wa.me/${wa}`;$('#whatsappLink').target='_blank';if($('#whatsappNumber'))$('#whatsappNumber').textContent=x.support_whatsapp||''}
 const grid=$('#gamesGrid');grid.innerHTML='';config.games.forEach(g=>grid.insertAdjacentHTML('beforeend',`<article class="game-card"><span class="badge">متوفر الآن</span><h3>${esc(g.icon)} ${esc(g.name)}</h3><p>${esc(g.description)}</p><a class="btn" href="#order" data-game="${g.id}">شحن ${esc(g.name)} ←</a><span class="emoji">${esc(g.icon)}</span></article>`));
 const gs=$('#gameId');gs.innerHTML='<option value="">اختر اللعبة</option>'+config.games.map(g=>`<option value="${g.id}">${esc(g.name)}</option>`).join('');
}
async function loadUser(){const {data}=await sb.auth.getUser();currentUser=data.user||null;let isAdmin=false;if(currentUser){const pr=await sb.from('profiles').select('role').eq('id',currentUser.id).maybeSingle();isAdmin=pr.data?.role==='admin'}document.querySelectorAll('.admin-link,a[href="./admin.html"]').forEach(x=>x.classList.toggle('hidden',!isAdmin));if(currentUser){$('#googleLogin').classList.add('hidden');$('#userMenu').classList.remove('hidden');$('#userName').textContent=`مرحبًا ${currentUser.user_metadata?.full_name||currentUser.email||'بك'}`;$('#walletGuest').classList.add('hidden');$('#walletPanel').classList.remove('hidden');await loadWallet();if(location.hash.slice(1)==='track')await loadTrackOrders()}else{$('#googleLogin').classList.remove('hidden');$('#userMenu').classList.add('hidden');$('#walletGuest').classList.remove('hidden');$('#walletPanel').classList.add('hidden');renderTrackGuest()}}
async function signIn(){const {error}=await sb.auth.signInWithOAuth({provider:'google',options:{redirectTo:location.origin+location.pathname}});if(error)show('#walletAlert','تعذر فتح تسجيل الدخول عبر Google: '+error.message,'error')}
$('#googleLogin').addEventListener('click',e=>{e.preventDefault();signIn()});$('#logoutUser').addEventListener('click',async()=>{await sb.auth.signOut();location.reload()});sb.auth.onAuthStateChange(()=>setTimeout(loadUser,0));
function fillPackages(){const id=Number($('#gameId').value),grid=$('#packagesGrid'),hidden=$('#packageId');hidden.value='';if(!id){grid.innerHTML='<div class="packages-empty">اختر اللعبة أولًا</div>';updatePrice();return}const packs=config.packages.filter(p=>p.game_id===id);grid.innerHTML=packs.length?packs.map(p=>`<button type="button" class="package-card" data-package-id="${p.id}"><span class="package-icon">✦</span><span class="package-name">${esc(p.name)}</span><span class="package-price">${esc(p.price_amount??p.price)} ${p.currency==='SYP'?'ل.س جديدة':'$'}</span>${p.price_usd?`<span class="package-usd">$${Number(p.price_usd).toFixed(2)}</span>`:''}<span class="package-check">✓</span></button>`).join(''):'<div class="packages-empty">لا توجد باقات متاحة لهذه اللعبة</div>';updatePrice()}
function updatePrice(){const p=config.packages.find(x=>x.id===Number($('#packageId').value));$('#orderPrice').textContent=p?`سيتم خصم ${money(p.price_amount??p.price,p.currency)} من محفظتك عند إرسال الطلب.`:'سيتم خصم قيمة الباقة من محفظتك عند إرسال الطلب.';if($('#orderPriceUsd'))$('#orderPriceUsd').textContent=p?.price_usd?`السعر بالدولار: $${Number(p.price_usd).toFixed(2)}`:''}
$('#gameId').addEventListener('change',fillPackages);document.addEventListener('click',e=>{const p=e.target.closest('.package-card');if(p){$('#packageId').value=p.dataset.packageId;document.querySelectorAll('.package-card').forEach(x=>x.classList.toggle('selected',x===p));updatePrice()}const g=e.target.closest('[data-game]');if(g){$('#gameId').value=g.dataset.game;fillPackages();openMainSection('order',true)}});
function orderNo(){return `BC-${new Date().toISOString().slice(0,10).replaceAll('-','')}-${Math.floor(100000+Math.random()*899999)}`}
const FREE_FIRE_KM_PRODUCTS=[
  {match:/110/,id:276},
  {match:/210\s*\+\s*21|231/,id:277},
  {match:/583/,id:14},
  {match:/1166|1188/,id:20},
  {match:/2398|2420/,id:23},
  {match:/الأسبوعية|weekly/i,id:27},
  {match:/الشهرية|monthly/i,id:31},
  {match:/booyah|pass/i,id:34}
];
function freeFireKmProductId(pack){const name=String(pack?.name||'');return FREE_FIRE_KM_PRODUCTS.find(x=>x.match.test(name))?.id||null}
async function submitFreeFireToKmCard(user,pack,playerId){
  const productId=freeFireKmProductId(pack); if(!productId) throw Error('هذه الباقة غير مربوطة بمنتج KM Card بعد');
  const cat=await sb.functions.invoke('kmcard-proxy',{body:{action:'products'}}); if(cat.error)throw cat.error;
  const product=(cat.data?.products||cat.data||[]).find(x=>Number(x.id)===productId);
  if(!product)throw Error('منتج KM Card غير متاح حاليًا');
  const key=String(product.params?.[0]||'ايدي اللاعب');
  const params={[key]:String(playerId).trim()};
  const idempotencyKey=crypto.randomUUID();
  const r=await sb.functions.invoke('kmcard-proxy',{body:{action:'order',product:{id:productId,name:product.name,category_name:product.category_name,price:Number(product.price),params:product.params||[]},qty:1,params,idempotency_key:idempotencyKey}});
  if(r.error)throw r.error; if(r.data?.error)throw Error(r.data.error);
  return r.data;
}
$('#orderForm').addEventListener('submit',async e=>{e.preventDefault();const btn=e.target.querySelector('button');btn.disabled=true;btn.textContent='جارٍ تنفيذ الطلب...';try{const {data:ud}=await sb.auth.getUser();if(!ud.user)throw Error('سجّل الدخول عبر Google أولًا قبل الشراء');const f=new FormData(e.target);if(!f.get('package_id'))throw Error('اختر الباقة أولًا');const pack=config.packages.find(x=>x.id===Number(f.get('package_id')));const game=config.games.find(x=>x.id===Number(f.get('game_id')));let result;if(game?.slug==='free-fire'){result=await submitFreeFireToKmCard(ud.user,pack,f.get('player_id'));show('#orderAlert',`تم إرسال طلب Free Fire إلى KM Card. رقم الطلب: ${result.order_number||'—'} — الحالة: ${result.status==='completed'?'مكتمل':result.status==='rejected'?'مرفوض وتم إرجاع الرصيد':'قيد المعالجة'}`,'success')}else{const r=await sb.rpc('purchase_with_wallet',{p_game_id:Number(f.get('game_id')),p_package_id:Number(f.get('package_id')),p_player_id:f.get('player_id'),p_customer_name:f.get('customer_name')});if(r.error)throw r.error;result={order_number:r.data.order_number};show('#orderAlert',`تم إنشاء الطلب وخصم الرصيد بنجاح. رقم طلبك: ${result.order_number}`,'success')}e.target.reset();$('#packageId').disabled=true;$('#trackNumber').value=result.order_number||'';await loadWallet();document.querySelector('#track').scrollIntoView({behavior:'smooth'})}catch(err){show('#orderAlert',err.message||'تعذر تنفيذ الطلب','error')}finally{btn.disabled=false;btn.textContent='إرسال الطلب ←'} });
function damascusDateParts(date=new Date()){const p=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Damascus',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(date);return Object.fromEntries(p.filter(x=>x.type!=='literal').map(x=>[x.type,x.value]))}function damascusMidnight(parts){const wall=Date.UTC(Number(parts.year),Number(parts.month)-1,Number(parts.day));const q=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Damascus',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).formatToParts(new Date(wall));const got=Object.fromEntries(q.filter(x=>x.type!=='literal').map(x=>[x.type,Number(x.value)]));const represented=Date.UTC(got.year,got.month-1,got.day,got.hour%24,got.minute,got.second);return new Date(wall-(represented-wall))}function trackMoney(pkg){if(!pkg)return '—';if(pkg.price_amount!=null)return money(pkg.price_amount,pkg.currency||'SYP');return pkg.price?esc(pkg.price):'—'}function renderTrackGuest(){const a=$('#trackAlert');if(a)show('#trackAlert','سجّل الدخول عبر Google لعرض طلبات اليوم','error');if($('#trackResult'))$('#trackResult').innerHTML='<h3>طلبات اليوم</h3><p class="muted">سجّل الدخول أولًا، وستظهر طلباتك تلقائيًا هنا.</p>'}async function loadTrackOrders(){if(!$('#trackResult'))return;if(!currentUser){renderTrackGuest();return}$('#trackAlert')?.classList.add('hidden');$('#trackResult').innerHTML='<h3>طلبات اليوم</h3><p class="muted">جارٍ تحميل طلباتك...</p>';const r=await sb.rpc('my_today_orders');if(r.error){show('#trackAlert','تعذر تحميل طلبات اليوم حاليًا','error');$('#trackResult').innerHTML='<h3>طلبات اليوم</h3><p class="muted">حدث خطأ أثناء تحميل طلباتك.</p>';return}const rows=r.data||[], labelsStatus={pending:'قيد الانتظار',processing:'قيد المعالجة',completed:'مكتمل',rejected:'مرفوض',refunded:'مرفوض — تمت إعادة الرصيد'};if(!rows.length){$('#trackResult').innerHTML='<h3>طلبات اليوم</h3><p class="muted">لا توجد طلبات لك اليوم.</p>';return}$('#trackResult').innerHTML=`<h3>طلبات اليوم <small class="muted">(${rows.length})</small></h3><div class="track-orders">${rows.map(d=>`<article class="track-order"><div><b>${esc(d.order_number)}</b><span class="status ${d.status==='completed'?'completed':d.status==='rejected'||d.status==='refunded'?'rejected':''}">${esc(labelsStatus[d.status]||d.status||'غير محدد')}</span></div><p>${esc(d.title||'—')} — ${esc(d.package_name||'—')}</p><small>المبلغ: ${money(d.amount||0,d.currency||'SYP')}<br>التاريخ: ${new Date(d.created_at).toLocaleString('ar-SY',{timeZone:'Asia/Damascus'})}${d.provider_status?`<br>حالة المزود: ${esc(d.provider_status)}`:''}</small></article>`).join('')}</div>`}$('#trackForm').addEventListener('submit',e=>{e.preventDefault();loadTrackOrders()});
$('#supportForm').addEventListener('submit',async e=>{e.preventDefault();const {data:ud}=await sb.auth.getUser(),f=Object.fromEntries(new FormData(e.target));const r=await sb.from('messages').insert({user_id:ud.user?.id||null,name:f.name,contact:f.contact,message:f.message});if(r.error)return show('#supportAlert','تعذر إرسال الرسالة','error');show('#supportAlert','تم إرسال رسالتك إلى الدعم','success');e.target.reset()});
const initialHash=location.hash.slice(1),initialTarget=sectionTargets[initialHash];openMainSection(initialTarget||null,false,initialTarget&&initialHash!==initialTarget?initialHash:'');Promise.all([loadConfig(),loadUser()]).then(()=>{const h=location.hash.slice(1),target=sectionTargets[h];openMainSection(target||null,false,target&&h!==target?h:'')}).catch(()=>show('#orderAlert','تعذر تحميل بيانات الموقع حاليًا','error'));
